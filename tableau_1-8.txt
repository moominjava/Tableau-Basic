태블로의 핵심 작동원리 이해

차원과 측정값

차원과 측정값을 이해하고 구분하는것이 가장 중요
태블로의 작동은 측정값을 차원으로 나누는 것에서 시작된다

측정값(Measure):우리가 관심을 가지는 대상 그 자체

*태블로에서 데이터를 불러올 경우 측정값이 차원으로 포함된 경우
	>>정수로 인식한경우이다
	>>정수로 인식되는것을 숫자로 바꿔준다

차원(Dimension): 어떻게 구분지을 것인가

